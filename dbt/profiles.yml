stock_research_agent:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "../data/reports.db"
      schema: "main"
      schemas_and_paths:
        main: "../data/reports.db"
      schema_directory: "../data/dbt_schemas"